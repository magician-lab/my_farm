name: Keep Render Awake

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - run: curl https://my-farm-donn.onrender.com/health
